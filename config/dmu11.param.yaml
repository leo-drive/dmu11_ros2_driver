/**:
  ros__parameters:

    working_frequency: 200 # working frequency of the driver
    publish_tf: false 
    
    serial_config:
      port: "/dev/ttyUSB0" # current port name of the device

    frame_config:
      imu_frame: "imu_link" # IMU frame name

      
